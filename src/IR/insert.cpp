#include "ir.hpp"


// std::expected<IR, Errors> insert_expr(Ast& ast, size_t* i) {
//     IrBuilder builder;

//     for (size_t count = *i; count < ast.size();count++) {
//         switch (ast[count].kind) {
//             case NodeKind::
//         }
//     }
// }